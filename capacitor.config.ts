import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.anonymous.insulinapp",
  appName: "InsulinApp",
  webDir: "build",
  plugins: {},
};

export default config;
